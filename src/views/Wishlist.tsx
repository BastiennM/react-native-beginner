import {Text, View} from 'react-native';
import * as React from 'react';

function Wishlist() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Wislist Screen</Text>
        </View>
    );
}

export default Wishlist;
